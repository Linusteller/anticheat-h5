# Anti-Cheat Detector v2 — 设计简报

## 产品定位

基于 WPK (WePoker) 逆向分析的 H5 反作弊检测工具。单个 HTML 文件，零依赖，浏览器打开即运行。目标用户：安全研究员、游戏风控工程师、反作弊方案评估人员。

---

## 核心架构：两阶段检测

### 阶段 1：设备画像（先回答"你是谁"）

在任何检测之前，先通过多信号交叉验证确定设备类型。这是整个系统的基础——不同设备有不同的正常行为，不能拿 Chrome 的标准去衡量 Safari。

**5 种设备类型**：
- 🍎 iPhone — Apple GPU + DeviceMotionEvent.requestPermission + 无 Battery API
- 🤖 Android — ARM 平台 + 移动 GPU (Adreno/Mali) + 移动 UA
- 💻 PC — 桌面 GPU (NVIDIA/AMD/Intel) + Win/Mac 平台
- ⚠️ 模拟器 — x86 平台运行 Android / 软件渲染 GPU / ARM GPU 在 x86
- ☁️ 云真机 — 数据中心 IP + 电池异常 + 无媒体设备

**硬证据短路**：存在确定性信号（如 x86 Android）时直接判定 100%，不走加权。

**输出**：设备类型 + 置信度百分比 + 各类型概率分布 + 硬证据列表

### 阶段 2：针对性风险检测（再回答"你安全吗"）

基于设备类型选择对应规则集。iPhone 不查 Battery API（本来就没有），PC 不查传感器（桌面端正常没有）。

**检测状态三级**：
- `pass`：检测通过，计入安全分
- `risk`：检测到风险，计入风险分
- `info`：采集/跳过/不可用，**不影响评分**（解决审查报告中"纯采集项虚增 pass"的问题）

### 评分公式

```
最终分 = 检测通过率 × 设备可信度

检测通过率 = pass / (pass + risk)     — 诚实反映检测能力
设备可信度 = 1 - max(模拟器置信度, 云真机置信度)  — 画像结论

效果：
  模拟器 100%: 91% × 0 = 0 分
  模拟器 74%:  92% × 0.26 = 24 分
  模拟器 23%:  88% × 0.77 = 68 分
  真机 0%:     100% × 1.0 = 100 分
```

---

## 检测项分类（约 49 项）

### 环境验证（8 项）
验证设备硬件环境是否真实。
- WebGL 渲染器（软件渲染 = 虚拟环境）
- GPU vs 平台一致性（ARM GPU 在 x86 = 模拟器翻译层）
- 电池状态（100% + 永充 + chargingTime=0 = 模拟器）
- 传感器（移动端无加速度计/陀螺仪/磁力计 = 模拟器）
- DeviceMotion 事件
- 硬件信息（核心数/内存/触摸点）
- 屏幕物理尺寸
- 媒体设备枚举（无摄像头+无麦克风 = 云真机）

### 指纹采集（6 项）
采集设备唯一特征，无基线对比时标为 `info`。
- Canvas 指纹
- AudioContext 指纹
- 字体指纹（26 种字体探测）
- UA/Platform 一致性
- 时区与语言
- WebGL 参数指纹

### 网络与位置（4 项）
- Geolocation（排除 0,0 和 1,1 假坐标）
- WebRTC IP 泄露
- 数据中心 IP 检测（调用 ipapi.co）
- Proxy/VPN 特征（时区 vs 语言不一致）

### 工具检测（5 项）
- 端口扫描（WPK 7125/7126 + Frida 27042/27043 + ADB 5555 + DevTools 9222）
- 自动化框架（WebDriver/Selenium/Phantom/ChromeDriver）
- Headless Chrome 深度检测（window.chrome 结构/Permissions/rtt/CDP 属性）
- DevTools 检测（窗口尺寸差 + console.log getter 陷阱）
- 浏览器版本新鲜度（仅对 Chrome 检查，Android Chrome < 115 = 模拟器特征）

### 完整性（4 项）
- 原生函数完整性（8 个核心函数 toString 检查）
- Console 劫持检测（WPK H5 覆写所有 console 方法）
- iframe 环境
- Service Worker

### 行为分析（7 项）
- WebView 环境检测（WPK 5 项子检测）
- 多开/多标签检测（BroadcastChannel）
- 属性一致性 Lie Detection（CreepJS 思想）
- Storage 可用性
- 页面生命周期
- FP/FCP/LCP 性能指标
- debugger 计时检测（GeoComply 反调试方法）

### v8 新增（6 项）
来源：WPK H5 v8 CDP 实测报告。
- eval 锁定检测
- CSP 安全策略
- 注入脚本检测
- XHR/WS 拦截检测
- matchMedia 特征（P3 色域/HDR/hover/pointer）
- debugger 计时

### 深度指纹（9 项）
来源：FingerprintJS BotD + CreepJS。
- Math 精度指纹
- WebGL 渲染指纹
- AudioContext 延迟
- 屏幕物理尺寸
- Vibration API
- Performance 计时
- 平台架构验证（x86 Android = 100% 模拟器）
- WebGL 扩展数量
- 时间源精度

### 综合判定（2 项）
- 设备类型概率
- 云真机综合判定（5 信号交叉）

---

## UI 页面结构

页面从上到下分为 5 个区域：

### 1. 头部
- 标题 "Anti-Cheat Detector v2"
- 副标题说明两阶段架构
- 进度条（检测完成百分比）

### 2. 设备画像卡片
检测开始后立即显示。这是用户第一眼看到的信息。

内容：
- 设备类型图标（🍎📱💻⚠️☁️）
- 设备类型名称（iPhone / Android / PC / 模拟器 / 云真机）
- 置信度或硬证据说明
- 标签条：GPU 型号、平台、核心数、内存、触摸点数

**设计要点**：
- 模拟器/云真机时整张卡片应有明显的红色/橙色警告感
- 真机时卡片应安全/绿色
- 硬证据时直接显示证据链（如 "x86平台运行Android + ARM GPU翻译层"）

### 3. 综合评分
圆环进度条 + 大数字分数 + 文字判定。

**分数颜色**：
- 80-100：绿色，"设备安全"
- 50-79：黄色，"存在风险"
- 0-49：红色

**判定文字**：
- 画像=安全设备 + 高分 → "设备安全"
- 画像=模拟器/云真机 → 显示 "模拟器 (置信度 XX%)" 红色，无论分数
- 低分 → "高风险设备"

**统计栏**：通过数（绿）| 风险数（红）| 信息数（青/灰）

### 4. 检测详情列表
按分类分组，每个检测项一张小卡片。

每张卡片：
- 左：检测项名称
- 右：状态徽章（通过/风险/信息）
- 下方：一行摘要文字
- 点击展开：详细 JSON 数据

**分类**：环境验证、指纹采集、网络与位置、工具检测、完整性、行为分析、v8 新增、深度指纹、综合判定

### 5. 底部操作栏
- "重新检测" 按钮
- "导出 JSON" 按钮

---

## 视觉风格建议

- **暗色主题**：深色背景（#0a0e17），适合安全工具的专业感
- **配色**：绿色(pass)、红色(risk)、青色(info)、蓝色(强调)
- **卡片风格**：圆角、微边框、半透明背景
- **字体**：系统字体栈，数字用粗体
- **响应式**：手机竖屏为主要使用场景（测试者在手机上打开），PC 居中最大宽度 640px
- **动画**：评分圆环有缓入动画，进度条平滑过渡

---

## 技术约束

- **单个 HTML 文件**：所有 CSS/JS 内联，零外部依赖
- **零框架**：原生 HTML/CSS/JS
- **兼容性**：Chrome 90+, Safari 15+, Firefox 90+
- **文件大小**：控制在 30KB 以内（纯文本，无图片）
- **离线可用**：除"数据中心 IP 检测"外全部本地运行

---

## 关键设计原则

1. **先识别再检测**：设备画像是基础，不同设备用不同规则
2. **诚实评分**：info 不影响分数，纯采集不算 pass，检测不到就承认
3. **硬证据优先**：x86 Android = 100% 模拟器，不走加权稀释
4. **画像联动评分**：模拟器置信度直接影响最终分数
5. **不误判真机**：iPhone 缺 Battery API 是正常的，不扣分
