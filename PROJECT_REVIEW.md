# 项目审查报告：Anti-Cheat Detector H5

审查日期：2026-05-12  
审查范围：`README.md`、`index.html`  
审查方式：静态代码审查 + 本地浏览器运行验证 + 参考 `REVIEW.md` 二次评估  
结论：本项目适合作为单文件 H5 反作弊检测 PoC，但当前不建议用于真实风控的拦截/放行决策。

## 总体评分

| 维度 | 分数 | 说明 |
| --- | ---: | --- |
| 综合评分 | 5.0 / 10 | 功能覆盖面较广，但核心判定和评分模型会稀释风险 |
| PoC / 演示价值 | 6.5 / 10 | 单文件、零依赖、界面完整，适合演示检测思路 |
| 真实反作弊价值 | 3.5 / 10 | 模拟器识别存在被 `pass/info` 稀释的系统性风险 |
| 工程化质量 | 2 / 10 | 无测试、无 CI、无模块拆分、无设备样本回归 |

## 参考 `REVIEW.md` 后的修订判断

- 采纳：`REVIEW.md` 对“模拟器得分被大量 `pass` 和 `info` 稀释”的分析成立，这比单纯说“评分偏乐观”更准确，应作为核心问题强调。
- 采纳：Math、Canvas、AudioContext、WebGL 渲染、Performance 等只采集 hash/耗时却直接标为 `pass`，会制造检测能力很强的错觉。
- 采纳：缺少高危信号一票否决或强权重机制，`navigator.webdriver`、Frida 端口、GPU/平台矛盾等风险可能被大量普通 `pass` 稀释。
- 部分采纳：`REVIEW.md` 中“真 Android 必有 Battery/Vibration API”的说法过于绝对，不宜直接作为单点 `risk`，但可作为组合风险信号。
- 不采纳：`REVIEW.md` 的综合评分 7.2 偏高；如果核心目标是反作弊，模拟器识别失效应拉低真实反作弊评分。
- 待验证：`REVIEW.md` 提到 MuMu/雷电实测得分 70-80，本次未在本机复现实测；但从代码路径和评分公式看，该风险具备较高可信度。

## 主要优点

- 单文件部署，打开成本低，适合快速演示和人工排查。
- 检测项覆盖 WebGL、Canvas、AudioContext、UA/Platform、WebRTC、端口扫描、自动化框架、完整性检查等多个浏览器信号。
- UI 信息组织清晰，有分类、详情展开、进度、评分和 JSON 导出。
- 针对 PC、Android、iPhone、模拟器、云真机做了初步设备画像。
- 代码依赖少，便于迁移、阅读和二次改造。

## 高优先级问题

### 1. `DeviceMotionEvent` 兼容性写法可能导致整页崩溃

位置：

- `index.html:170`
- `index.html:296`
- `index.html:318`

问题：

```js
typeof DeviceMotionEvent?.requestPermission === 'function'
```

如果浏览器环境里不存在 `DeviceMotionEvent`，该表达式会抛出 `ReferenceError`。由于 `index.html:170` 位于 `identifyDevice()` 初始化阶段，一旦触发会导致后续检测无法启动。

影响：

- 兼容性风险高。
- 在不支持 DeviceMotion API 的浏览器或特殊 WebView 中可能白屏/停止检测。

建议：

- 使用 `window.DeviceMotionEvent` 进行安全访问。
- 把能力检测封装成独立 helper，避免类似错误重复出现。

### 2. 评分模型会稀释模拟器风险，不适合作为风控结论

位置：

- `index.html:141`

当前评分逻辑：

```js
score = pass / (pass + risk)
```

问题：

- `info` 完全不影响分数。
- 很多实际未检测或跳过的项目被标为 `pass`。
- 桌面端“无需传感器”“无需运动传感器”等跳过项也计入通过。
- 外部 IP 检测失败、权限拒绝、API 不可用时，整体分数仍可能接近 100。
- 纯采集型检测直接返回 `pass`，会给模拟器、Headless 或伪装环境“送分”。
- 缺少一票否决或强权重，高危 `risk` 会被几十个低价值 `pass` 稀释。

验证现象：

- 使用 Headless Chrome 本地跑完 43 项后页面显示 100 分。
- 同时日志中存在 `ipapi.co` CORS 错误、`navigator.vibrate` 被浏览器拦截等问题。
- 这说明分数不能真实反映检测完整性和风险可信度。

影响：

- 容易把“未检测到风险”误解释为“设备安全”。
- 不利于真实风控场景做拦截、降权或人工复核。

建议：

- 区分 `pass`、`risk`、`info`、`skip`、`blocked`、`error`。
- 按检测项价值加权，不应简单按数量平均。
- 对关键检测不可用的情况降低置信度，而不是忽略。
- 把无基线的纯采集项改为 `info`，不要计入安全分。
- 对自动化框架、Frida 端口、GPU/平台矛盾等高危信号设置强扣分或一票否决。

### 3. README 声称“双击即开”，但外部 IP 检测在 `file://` 下不可用

位置：

- `README.md:5`
- `index.html:454`

问题：

README 写明：

```md
单个 HTML 文件，零依赖，双击即开。
```

但代码中存在：

```js
fetch('https://ipapi.co/json/')
```

在 `file://` 环境下，本地验证出现 CORS 拦截，导致“数据中心 IP”检测失败。

影响：

- “数据中心 IP”“云真机判定”“Proxy/VPN”相关能力会被削弱。
- README 使用说明与实际运行效果不一致。

建议：

- 明确要求通过本地 HTTP 服务运行，例如 `python -m http.server`。
- 或移除/替换会被 `file://` CORS 限制的检测方式。
- 或把该检测标记为“需要网络与 CORS 支持”。

## 中优先级问题

### 4. `WebRTC IP` 存在永远不上报的分支

位置：

- `index.html:446`

问题：

超时分支只调用了 `rv()`，没有调用 `report()`：

```js
setTimeout(()=>{try{pc.close()}catch(e){}rv()},3000);
```

如果浏览器没有触发 `onicecandidate(null)`，该检测项可能一直显示“检测中”。

影响：

- 进度条可能无法到 100%。
- 评分统计可能缺项。
- 用户无法判断该项是通过、失败还是超时。

建议：

- 超时时也必须调用 `report('WebRTC IP', 'info', '检测超时', ...)`。
- 所有异步检测都应保证 exactly-once 上报。

### 5. 使用 `innerHTML` 渲染环境标签，有 DOM 注入风险

位置：

- `index.html:241`

问题：

```js
tags.innerHTML = p.tags.map(t=>`<span class="tag">${t}</span>`).join('');
```

`p.tags` 中包含 GPU、platform 等环境字符串。在反作弊场景中，这些值可能被自动化框架、扩展或注入脚本伪造，不应直接拼接为 HTML。

影响：

- 存在 DOM XSS / DOM 注入风险。
- 在安全检测工具里尤其不应信任客户端环境字段。

建议：

- 改用 `document.createElement()` 和 `textContent`。
- 所有来自浏览器环境、UA、GPU、IP、语言等字段都按不可信输入处理。

### 6. 文档宣称的部分检测源码未实现

位置：

- `README.md:15`
- `README.md:17`

README 声称包含：

- 触摸事件来源 `isTrusted`
- 按键事件注入
- 插件数量

但源码中未找到对应检测实现。

影响：

- 文档夸大检测范围。
- 容易让使用者误判项目能力。

建议：

- 要么补齐对应检测。
- 要么从 README 中删除未实现描述。
- 文档应区分“已实现”“计划实现”“仅参考 WPK 思路”。

## 低优先级问题

### 7. 端口扫描会产生大量浏览器控制台噪声

位置：

- `index.html:477`

问题：

通过 WebSocket 扫描本地端口会在控制台产生大量连接失败、握手失败、403 等日志。

影响：

- 影响调试体验。
- 在某些浏览器策略或安全插件下可能被拦截。
- 用户可能误以为页面报错。

建议：

- 在 UI 中说明这是预期行为。
- 降低扫描频率或允许用户手动触发。
- 把端口扫描结果和浏览器控制台错误区分开。

### 8. 多数“深度指纹”目前只是采集，不具备判定能力

位置：

- `index.html:652`
- `index.html:673`
- `index.html:733`

问题：

Math、WebGL 渲染、Performance 等指纹项目前大多只计算 hash 或耗时，然后直接标为 `pass`。

影响：

- 看起来检测项很多，但实际风险判断能力有限。
- 缺少真实设备、模拟器、云真机、Headless 的基准样本，无法证明有效性。

建议：

- 建立样本库。
- 按设备类型、浏览器版本、GPU、系统版本记录指纹分布。
- 在没有基准前，这类项目应标为 `info` 而不是 `pass`。

## 验证记录

### 语法检查

执行方式：

```powershell
# 从 index.html 提取内联 script 后执行
node --check <temp-js-file>
```

结果：

- JavaScript 语法检查通过。

### 浏览器运行验证

执行环境：

- Windows
- Chrome Headless
- 本地 `file://` 打开 `index.html`

观察结果：

- 页面完成 43 项检测。
- 页面显示 41 个通过、0 个风险、2 个信息项。
- 页面最终评分为 100。
- 浏览器日志出现 `ipapi.co` CORS 错误。
- 浏览器日志出现 WebSocket 本地端口扫描失败/握手失败信息。
- 浏览器日志出现 `navigator.vibrate` 因无用户手势被拦截。

结论：

- 页面基本能跑通。
- 但 100 分结果明显偏乐观，不能代表真实安全性。

## 建议路线

### 第一阶段：修稳定性

- 修复 `DeviceMotionEvent` 安全访问。
- 修复所有异步检测必须上报的问题。
- 为网络失败、权限拒绝、API 不可用设置明确状态。
- 避免把跳过项计入 `pass`。

### 第二阶段：修可信度

- 重做评分模型。
- 引入置信度字段。
- 将纯采集型指纹项从 `pass` 调整为 `info`。
- 为关键检测不可用设置扣分或降置信度。

### 第三阶段：补工程化

- 增加 Playwright 或 Chrome DevTools Protocol 自动化测试。
- 加入典型场景测试：普通桌面、Headless、移动 UA、WebView、缺失 API、外部网络失败。
- 拆分检测项，避免 800 多行逻辑全部堆在单个 HTML 中。
- 建立真实设备与模拟器样本数据。

## 最终结论

这个项目作为 H5 反作弊检测演示是有价值的：界面完整、覆盖面广、部署简单，适合展示检测思路和采集环境信号。

但如果目标是生产级反作弊，当前版本不能作为拦截/放行依据。核心短板不是检测项数量，而是：

- 检测失败没有被正确反映到评分。
- 很多采集项没有基准，不能真正判定风险。
- 模拟器风险会被大量低价值 `pass` 和 `info` 稀释。
- 缺少高危信号的一票否决或强权重机制。
- 文档宣称与源码实现不完全一致。
- 缺少自动化测试和多设备样本验证。

建议定位为 PoC。只有在修复稳定性问题、重做评分模型、建立样本基准并通过真实设备/模拟器回归测试后，才适合考虑接入真实风控流程。
